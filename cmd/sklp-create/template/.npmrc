# @lalternative packages are published to the public npmjs.org registry —
# installed anonymously, no override needed. @digstack packages stay on GitHub
# Packages (private) and need GITHUB_TOKEN.
@digstack:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
